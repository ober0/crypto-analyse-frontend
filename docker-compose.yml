version: "3.9"

services:
  frontend-everyday-forecast:
    build: .
    container_name: frontend-everyday-forecast
    env_file:
      - .env
    ports:
      - "${VPS_PORT}:3000"
    restart: always